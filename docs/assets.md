# 声音与视觉资源

- rain.wav：原创程序化雨声，30 秒、22050 Hz、16-bit 单声道 PCM。运行 `node scripts/generate-rain.mjs` 可重建；无第三方录音，CC0。声音不是实地录音，不宣称治疗作用。
- 图标：lucide-react（ISC），界面中的情绪表情为自绘 SVG。
- 字体：系统字体，无外部字体请求。
- 用户已选择文字设计，不生成图片概念稿；视觉验收依据批准的设计文档。
