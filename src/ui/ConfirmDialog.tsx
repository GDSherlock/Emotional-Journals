import {useEffect,useRef,type ReactNode} from 'react';
export function ConfirmDialog({title,children,onCancel,onConfirm,busy=false}:{title:string;children:ReactNode;onCancel:()=>void;onConfirm:()=>void;busy?:boolean}){const ref=useRef<HTMLDialogElement>(null);useEffect(()=>{const previous=document.activeElement as HTMLElement|null;ref.current?.showModal();return()=>previous?.focus();},[]);return <dialog ref={ref} className="dialog" aria-labelledby="dialog-title" onCancel={e=>{e.preventDefault();if(!busy)onCancel();}}><h2 id="dialog-title">{title}</h2><div>{children}</div><div className="actions"><button autoFocus onClick={onCancel} disabled={busy}>取消</button><button className="primary" onClick={onConfirm} disabled={busy}>{busy?'处理中…':'确认'}</button></div></dialog>;}
